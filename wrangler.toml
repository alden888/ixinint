name = "ixinint"
compatibility_date = "2025-12-07"
compatibility_flags = ["nodejs_compat"]

# 静态网站配置 - 所有文件都在根目录
[assets]
directory = "./"  # 关键：指向根目录，不是 public

# 构建配置
[build]
command = "echo 'Static site - no build needed'"